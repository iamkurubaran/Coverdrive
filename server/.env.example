# Optional — lifts GitHub from 60 to 5,000 requests/hour.
# Create a fine-grained token with no scopes at https://github.com/settings/tokens
# and export it before starting the server:
#
#   GITHUB_TOKEN=ghp_xxx node index.js
#
GITHUB_TOKEN=
PORT=8787
CACHE_TTL_MS=600000
